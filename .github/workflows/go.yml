name: Go

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: '1.24.4'

    - name: Print Go Envs
      run: go env
        
    - name: Build
      run: go build -v -o ./uuidgen/cmd/uuidgen ./uuidgen/cmd/uuidgen.go

    - name: Test
      run: go test -race -v ./uuidgen/...